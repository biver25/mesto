import PopupWithForm from './PopupWithForm.js';

export default class PopupWithFormAvatar extends PopupWithForm {
  constructor (popupSelector, submitHandler) {
    super(popupSelector, submitHandler);
  }
}
